# v0.7.1

- 知識查核 skill 改名為 `plasma-knowledge-lookup`，skills 內統一以「Plasma 知識庫」稱呼知識服務。
- `plasma-create-pview` 明訂不能略過 mview：pview 只能讀取本流程建立或重用的 mview，並在 `create_pview` 前逐項核對。
- 完成 pview 後，在對話中整理本次 SQL 的輸出欄位表與參數說明。

手動安裝升級時移除舊 `ophion-knowledge-lookup` 目錄，並重新開啟 session。
