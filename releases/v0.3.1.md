# v0.3.1 — 僅建立 view／mview

- 移除 `plasma-export` 與 `plasma-data-api`，統一為 `plasma-create-view`。
- 唯一路徑：Ophion 查核 → Trino SQL 驗證 → 建立 view／manual mview → 核對定義後結束。
- 移除建立 blueprint、匯出檔案／外部資料庫、同步與 API 發布的工作流程。
- mview 固定建立 manual 定義，不以 scheduled 建立觸發同步。
- 更新 setup、知識查核、雙平台 manifests、封裝與後端待辦，兩包均只有三份 skills。
- 更新後開新對話／session，避免沿用已載入的舊版流程。

本次只調整 plugin 與後端待辦文件，不刪除共用後端既有工具或 API。
