# v0.3.0 — ChatGPT 與 Claude 共用工作流程

- 新增 ChatGPT 原生 manifest，保留 Claude manifest 與 plugin 名稱。
- 共用四份 skills；setup 改為宿主連線／OAuth 診斷，不再要求本機設定。
- 移除 PreToolUse hook、Go 程式、本機 launcher、binary 下載及快取。
- 同步與發布前的確認保留為對話／宿主流程，不再宣稱有 hook 強制攔截。
- 改為發佈 ChatGPT／Claude ZIP 與 SHA-256 checksums，無平台 binary。
- 可用真實 app ID 產生工作區 app 綁定包；預設包不含 app ID 或 MCP 宣告。
- 修正追加權限說明、知識 profile 差異與 blueprint 核對欄位不足時的處理。
- 整理後端修改與端到端驗收清單：`docs/backend-integration.md`。

升級後重新開啟對話／session；曾手動設定舊 hook 者需移除自訂註冊。
本版本完成 plugin 封裝遷移；後端追加授權及 ChatGPT 實際連線仍需依清單驗收。
